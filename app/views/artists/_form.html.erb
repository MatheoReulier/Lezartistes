<%= form_with(model: artist) do |form| %>
  <% if artist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
        <% artist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom_de_scène, style: "display: block" %>
    <%= form.text_field :nom_de_scène %>
  </div>

  <div>
    <%= form.label :domaine_artistique, style: "display: block" %>
    <%= form.text_field :domaine_artistique%>
  </div>

  <div>
    <%= form.label :biographie_et_réalisations, style: "display: block" %>
    <%= form.text_area :biographie_et_réalisations %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_area :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
